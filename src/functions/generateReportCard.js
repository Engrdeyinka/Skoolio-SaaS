export const generateReportCard = async (params) => {
  console.warn('generateReportCard: Edge function not yet deployed');
  return { status: 200, data: { message: 'Report card generation not yet configured' } };
};
